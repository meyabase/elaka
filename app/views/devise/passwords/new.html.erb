<br>
<br>
<div class="card">
  <div class="col-md-9 align-self-center">
    <div class="card-body">
    <div class="d-flex flex-column">
      <br>
      <h5 class="text-secondary text-center">Forgot your password?</h5>
      <hr class="custom-horizontal">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name)) do |f| %>
        <div class="form-group">
          <div class="field">
            <%= f.email_field :email, class: 'form-control session-form', placeholder: 'email',
                             autocomplete: "Enter email address", autofocus: true %>
            <% resource.errors.full_messages_for(:email).each do |message| %>
              <small class="help-block text-danger"><%= message %></small>
            <% end %>
          </div>
        </div>

        <div class="form-group">
          <div class="actions">
            <%= f.submit "Send", class: "btn btn-primary btn-block" %>
          </div>
        </div>
      <% end %>

      <hr class="custom-horizontal">

      <div class="signup-section">
        <div class="text-dark text-center">
          <%= link_to 'Sign In', new_session_path(resource_name) %>
          Or <%= link_to 'Join Now', new_registration_path(resource_name) %>
        </div>
        <p class="text-center">
          <%= link_to 'Need Help?', help_path %>
        </p>
      </div>
    </div>
  </div>
  </div>
</div>
